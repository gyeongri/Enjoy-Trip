<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip.board.model.mapper.BoardMapper">

	<resultMap type="boardDto" id="article">
		<result column="article_type" property="articleType"/>
		<result column="article_no" property="articleNo"/>
		<result column="article_title" property="articleTitle"/>
		<result column="user_id" property="userId"/>
		<result column="user_nickname" property="userNickname"/>
		<result column="article_content" property="articleContent"/>
		<result column="article_hit" property="articleHit"/>
		<result column="article_register_time" property="articleRegisterTime"/>
	</resultMap>
	
	
	<resultMap type="commentDto" id="comment">
		<result column="comment_no" property="commentNo"/>
		<result column="user_id" property="userId"/>
		<result column="comment_content" property="commentContent"/>
		<result column="comment_register_time" property="commentRegisterTime"/>
		<result column="article_no" property="articleNo"/>
		<result column="user_nickname" property="userNickname"/>
	</resultMap>	
	
<!-- 	<resultMap type="commentDtoList" id="commentList">
		<result column="comment_no" property="commentNo"/>
	</resultMap> -->	
	
		<!-- 댓글 쓰기 -->
	<insert id="writeComment" parameterType="commentDto">
		insert into comment (user_id, comment_content, comment_register_time, article_no)
		values (#{userId}, #{commentContent}, now(), #{articleNo})
		<selectKey resultType="int" keyProperty="commentNo" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>
	
	
	<!-- 댓글 보기 -->
	<select id="listComment" parameterType="int" resultMap="comment">
		select c.comment_no, u.user_nickname, c.comment_content,c.comment_register_time
		from comment c join board b join user u
		on c.article_no=b.article_no and u.user_id=c.user_id
		where b.article_no = #{articleNo}
		order by comment_register_time 		
	</select>	
	
	
	<!-- 댓글 삭제 -->
	<delete id="deleteComment" parameterType="int">
		delete from comment
		where comment_no = #{commentNo}
	</delete>	
	
	
	<!-- 글 보기 -->
	
<!-- 	<select id="getArticle" parameterType="int" resultMap="viewArticle">
		select b.article_no, b.user_id, b.article_title, b.article_content, b.article_hit, b.register_time, m.user_name 
		from board b, members m
		where b.user_id = m.user_id
		and b.article_no = #{articleNo}
	</select> -->
	
<!-- 		<resultMap type="boardDto" id="viewArticle" extends="article">
		<collection property="fileInfos" column="article_no" javaType="list" ofType="fileInfoDto" select="fileInfoList"/>
		</resultMap> -->
	
	<select id="getArticle" parameterType="int" resultMap="article">
		select b.article_no, b.article_title, u.user_nickname, b.article_content, b.article_hit, b.article_register_time
		from board b join user u
		on b.user_id=u.user_id
		where b.article_no = #{articleNo};
	</select>
	
	<resultMap type="fileInfoDto" id="file">
		<result column="file_id" property="fileId"/>
		<result column="article_no" property="articleNo"/>
		<result column="file_path" property="filePath"/>
	</resultMap>

	<!-- 글 쓰기 -->
	<insert id="writeArticle" parameterType="boardDto">
		insert into board (user_id, article_title, article_content, article_hit, article_register_time)
		values (#{userId}, #{articleTitle}, #{articleContent}, 0, now())
		<selectKey resultType="int" keyProperty="articleNo" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>
	
	<!-- 파일 등록 -->
	<insert id="registerFile" parameterType="boardDto">
		insert into file_info (article_no, save_folder, original_file, save_file)
		values
		<foreach collection="fileinfos" item="fileinfo" separator=" , ">
			(#{articleNo}, #{fileinfo.saveFolder}, #{fileinfo.originalFile}, #{fileinfo.saveFile})
		</foreach>
	</insert>

	<sql id="search">
		<if test="word != null and word != ''">
			<if test="key == 'article_title'">
				and article_title like concat('%', #{word}, '%')
			</if>
			<if test="key == 'user_nickname'">
				and ${key} like concat('%', #{word}, '%')
			</if>
			<if test="key == 'article_no'">
				and ${key} = #{word}
			</if>
			
		</if>
		<if test="key == null">
			and (article_title like concat('%', #{word}, '%')
			or article_no like concat('%', #{word}, '%')
			or user_nickname like concat('%', #{word}, '%'))
		</if>
	</sql>


	<!-- 전체 글 조회  -->


	<select id="listArticle" parameterType="map" resultMap="article">
		select b.article_type, b.article_no, b.article_title, u.user_nickname, b.article_hit, b.article_register_time
		from board b join user u
		on b.user_id=u.user_id
		<include refid="search"></include>
		order by b.article_no desc
		limit #{start}, #{listsize}
	</select>
	
	<!-- 전체 글 수 세기 -->
	<select id="getTotalArticleCount" parameterType="map" resultType="int">
		select count(article_no)
		from board
		<where>
			<include refid="search"></include>
		</where>
	</select>
	

	
	<select id="fileInfoList" resultMap="file">
		select save_folder, original_file, save_file
		from file_info
		where article_no = #{articleNo}
	</select>
	
	<update id="updateHit" parameterType="int">
		update board
		set article_hit = article_hit + 1
		where article_no = #{articleNo}
	</update>
	
	<update id="modifyArticle" parameterType="boardDto">
		update board
		set article_title = #{articleTitle}, article_content = #{articleContent}
		where article_no = #{articleNo}
	</update>
	
	<delete id="deleteFile" parameterType="int">
		delete from file_info
		where article_no = #{articleNo}
	</delete>
	
	<delete id="deleteArticle" parameterType="int">
		delete from board
		where article_no = #{articleNo}
	</delete>

</mapper>